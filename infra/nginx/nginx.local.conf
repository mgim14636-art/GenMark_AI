worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;
    client_max_body_size 50M;

    # Docker recreates service containers during a rebuild, which can change
    # their IP addresses. Re-resolve service names so API traffic does not
    # remain pinned to a stopped backend container.
    resolver 127.0.0.11 valid=10s ipv6=off;

    upstream backend_app {
        zone backend_app 64k;
        server backend:8081 resolve;
    }

    server {
        listen 80;
        server_name localhost;

        # React SPA static files
        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html;
            add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
            # 구글 로그인 팝업이 postMessage로 결과를 돌려줄 수 있게 허용
            # (COOP 기본값이 이 팝업 통신을 막아서 "버튼을 준비하지 못했어요" 오류가 났었음).
            add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
        }

        # Always fetch the latest bundle manifest after a frontend rebuild.
        location = /index.html {
            root /usr/share/nginx/html;
            add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
            add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
        }

        # Hashed Vite assets are safe to cache because their filenames change per build.
        location /assets/ {
            root /usr/share/nginx/html;
            add_header Cache-Control "public, max-age=31536000, immutable" always;
        }

        # Spring Boot API routes
        location /api/ {
            proxy_pass http://backend_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Generated logo files stored by Spring Boot
        location /uploads/ {
            proxy_pass http://backend_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
